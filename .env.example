# Environment Variables Configuration for Firework Factory

# Copy this file to .env.local and configure your actual values
# .env.local is gitignored and won't be committed to your repository

# =============================================================================
# ERROR MONITORING CONFIGURATION
# =============================================================================

# Discord Webhook URL for error notifications
# To get this:
# 1. Go to your Discord server settings
# 2. Navigate to Integrations > Webhooks
# 3. Click "Create Webhook" or "New Webhook"
# 4. Name it (e.g., "Firework Factory Errors")
# 5. Choose the channel where errors should be posted
# 6. Copy the webhook URL
VUE_APP_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# GitHub Personal Access Token for creating issues
# To get this:
# 1. Go to GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 2. Click "Generate new token (classic)"
# 3. Add note: "Firework Factory Error Reporting"
# 4. Select scopes: "repo" (for private repos) or "public_repo" (for public repos)
# 5. Generate token and copy it
VUE_APP_GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# GitHub Repository (format: owner/repo)
# Replace with your actual GitHub username and repository name
VUE_APP_GITHUB_REPO=your-username/your-repo-name

# Application Version (for error tracking)
VUE_APP_VERSION=1.0.0

# =============================================================================
# FIREBASE CONFIGURATION (if needed)
# =============================================================================

# Add your Firebase config here if you want to use Firebase for data storage
# VUE_APP_FIREBASE_API_KEY=your_firebase_api_key
# VUE_APP_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
# VUE_APP_FIREBASE_PROJECT_ID=your_project_id
# VUE_APP_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
# VUE_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
# VUE_APP_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# =============================================================================
# REMOTE DEPLOYMENT CONFIGURATION
# =============================================================================

# Server details for remote deployment (if using self-hosted runner)
# VUE_APP_DEPLOY_SERVER=your-server.com
# VUE_APP_DEPLOY_PATH=/path/to/your/app
# VUE_APP_DEPLOY_USER=deploy_user

# =============================================================================
# DEVELOPMENT/PRODUCTION SETTINGS
# =============================================================================

# Set to 'production' when deploying
NODE_ENV=development
